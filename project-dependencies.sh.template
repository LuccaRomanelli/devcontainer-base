#!/bin/bash
# ============================================================
# Project Dependencies Template
# ============================================================
# Copie este arquivo para project-dependencies.sh e customize
# para instalar dependências específicas do seu projeto.
#
# Este script é executado automaticamente pelo post-create.sh
# ============================================================
set -e

echo "==> Installing project-specific dependencies..."

# ------------------------------------------------------------
# Runtimes via mise (recomendado)
# ------------------------------------------------------------
# mise use python@3.11
# mise use node@20
# mise use go@1.21

# ------------------------------------------------------------
# Pacotes via Homebrew
# ------------------------------------------------------------
# brew install sqlite
# brew install postgresql@15
# brew install redis

# ------------------------------------------------------------
# Bibliotecas do Playwright (se necessário browser automation)
# ------------------------------------------------------------
# sudo apt-get update && sudo apt-get install -y \
#     libnss3 libnspr4 libatk1.0-0 libatk-bridge2.0-0 \
#     libcups2 libdrm2 libxkbcommon0 libxcomposite1 \
#     libxdamage1 libxfixes3 libxrandr2 libgbm1 \
#     libasound2 libpango-1.0-0 libcairo2 libatspi2.0-0

# ------------------------------------------------------------
# Dependências do projeto
# ------------------------------------------------------------
# pip install -r requirements.txt
# npm install

# ------------------------------------------------------------
# Ferramentas de desenvolvimento
# ------------------------------------------------------------
# playwright install chromium --with-deps

echo "==> Project dependencies installed!"
